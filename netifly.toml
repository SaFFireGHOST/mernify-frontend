[build]
  # For Vite:
  command = "npm run build"
  publish = "dist"
  # For CRA use publish="build"

# SPA fallback: send all non-file paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
